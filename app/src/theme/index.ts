// NovaPay Theme - Main Export
import { colors, lightColors, darkColors } from './colors';
import { typography } from './typography';
import { spacing, shadows } from './spacing';
import { animations, borderRadius, zIndex } from './animations';

export const theme = {
    colors,
    typography,
    spacing,
    shadows,
    animations,
    borderRadius,
    zIndex,
};

export { colors, lightColors, darkColors, typography, spacing, shadows, animations, borderRadius, zIndex };
export default theme;
